import { rubyChina } from '../../../helpers/ruby-china';
import { module, test } from 'qunit';

module('Unit | Helper | ruby china');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = rubyChina(42);
  assert.ok(result);
});
