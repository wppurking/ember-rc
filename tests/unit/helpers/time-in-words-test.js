import { timeInWords } from '../../../helpers/time-in-words';
import { module, test } from 'qunit';

module('Unit | Helper | time in words');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timeInWords(42);
  assert.ok(result);
});
