import nodeSection from '../../../utils/node-section';
import { module, test } from 'qunit';

module('Unit | Utility | node section');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = nodeSection();
  assert.ok(result);
});
